using UnityEngine;

public class Aardvark : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Test2()
    {
        Debug.Log("Grabbed!!");
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
